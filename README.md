# react-xray

Per-page component reachability for React apps.

Static analysis: see which pages render which components, which components no page reaches, and how third-party libraries are used.

**Status:** v0 in development.
